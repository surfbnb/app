import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  backgroundStyle: {
    backgroundColor: '#fff',
    flex: 0.5,
    alignItems: 'center',
    justifyContent: 'flex-end',
    padding: 30,
    marginTop: 200
  }
});
