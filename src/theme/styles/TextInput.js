import { StyleSheet } from 'react-native';

export default TextInput = StyleSheet.create({
    textInputStyle :{
        borderColor: '#E9E9E9',
        borderWidth: 1,
        borderRadius:5,
        fontSize : 15,
        padding :5,
        paddingLeft : 10,
        marginBottom: 12
    }
});